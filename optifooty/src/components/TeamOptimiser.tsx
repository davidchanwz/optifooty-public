import React from 'react';
import './TeamOptimiser.css'; // Create a CSS file for styling

const TeamOptimiser: React.FC = () => {
    return (
        <div className="team-optimiser">
            <h2>Team Optimiser</h2>
            <p>This is where the team optimiser component will be displayed.</p>
        </div>
    );
};

export default TeamOptimiser;